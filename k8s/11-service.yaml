apiVersion: v1
kind: Service
metadata:
  name: sample-app
  namespace: platform
spec:
  selector:
    app: sample-app
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: LoadBalancer
